import React, { Component } from 'react';

class Header extends Component {
    render() {
        return (
            <div className="App">
             some text from header
            </div>
    );
    }
}

export default Header;
